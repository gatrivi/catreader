# Handoff — 2026-08-02

**Version:** `v2.10.19` · branch `main` = `master/main` (pushed)

## Done this session
1. Reconciled ahead/behind: dropped duplicate Discover commits, kept WIP on remote tip.
2. `v2.10.17` — feed diversify + TTS running-header strip + pdfParser header band.
3. `v2.10.18` — dim Discover parchment (too bright).
4. `v2.10.19` — Discover ≈ real Paper Soul: grain + stains + ink variance; procedural stain fallback when bake missing (only README + Cloud of Unknowing baked).

## Open
- Full library Paper Soul bake (`npm run paper-bake` / catts job) — procedural fallback is stand-in.
- Photo grain tiles (plan phase 4) still SVG turbulence.
- DnD ghost, shelf nesting, live audio EPUB — unchanged known issues.

## Sacred
- Progress: `docs/PROGRESS_SACRED.md` — never clobber page on remount.
- Reader mode lazy: `docs/READER_MODE_LAZY.md`.

## Verify
`npm test` → 118 pass. UI corner badge = `v2.10.19`.
